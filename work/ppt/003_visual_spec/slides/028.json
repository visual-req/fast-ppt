{
  "layout_type": "svg_full",
  "title": "验收用例生成：为什么场景和验收用例天然匹配？",
  "svg": {
    "src": "work/assets/003/018-accept-scenario-match.svg",
    "alt": "场景矩阵到验收用例的天然映射关系",
    "caption": "场景矩阵的每一行 = 角色 + 操作 + 条件 + 预期，验收用例 = Given（前置条件）+ When（操作步骤）+ Then（预期结果 + 通过标准）。两者一一对应：正常路径 → Happy Path 测试，边界条件 → 边界测试，异常场景 → 异常测试。同构性、完整性、一致性确保不遗漏不重复。"
  }
}
